<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CityVisitorFinaly.MapsPage"
             Title="MapsPage"
             xmlns:skia="clr-namespace:SkiaSharp.Views.Maui.Controls;assembly=SkiaSharp.Views.Maui.Controls">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>


        <ContentView x:Name="comicPageContainer">
            <!-- Просто для примера, если ожидается, что canvasView будет занимать определенные размеры -->
        <skia:SKCanvasView x:Name="canvasView" Grid.RowSpan="2" />
        </ContentView>
    </Grid>
</ContentPage>